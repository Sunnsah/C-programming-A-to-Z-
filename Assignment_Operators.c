#include <stdio.h>
int main()
{
    int a=7,b=8,c=10,d=20,e=25, f= 55, g= 90, h=75,i= 100;

    // a+=b; //Same as a+b:
    i/=e; //Same as i / e:
    e%=a;

    printf("After A is increment : %d  \n",e);
    return 0;
}